apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090/prometheus/ # http://localhost:9090/prometheus/
    isDefault: true
    editable: true